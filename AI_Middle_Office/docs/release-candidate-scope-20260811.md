# 2026-08-11 正式发布候选范围

## 当前结论

本次发布候选以 Windows 本地 `127.0.0.1:9000` 已验收版本为基础，但不得直接复制当前工作目录到 ECS。发布前必须完成全量回归、生产镜像构建、数据库备份与恢复演练、迁移预演和上线后冒烟测试。

当前 Git 基线为 `36ad56f`，本地改动尚未形成发布提交。代码 Alembic 唯一 head 为 `20260811_0091`；正式 ECS 数据库最近一次只读记录为 `20260808_0082`，正式操作前必须重新核对。

## 纳入候选的用户可见功能

- 项目报价页面布局、清单项/定额项/明细项三级交互及明细字段编辑。
- 项目定额新增、多选新增、删除、替换及金额、总计和统计信息联动。
- 企业定额主库悬浮查看、定额匹配查看、当前 active 版本选择和工料机明细解析。
- 人工、主材、辅材、机械子行识别和 `.xls/.xlsx/.xlsm` 导入解析。
- 原格式报价 Excel 导出、工作表识别和统计导出修复。
- 报价来源展示、企业定额定位和项目报价页面紧凑布局。
- 账号功能授权增删、实际可用功能展示和角色兼容处理。
- 新建用户首次登录强制改密，以及业务页面收到 `PASSWORD_CHANGE_REQUIRED` 时自动进入改密窗口并保留原目标地址。
- 报价金额一致性、并发保护、生产配置校验、日志脱敏和单 ECS 部署安全文件。

## 隔离但保留测试的功能

- 报价资料研判新数据域 `20260810_0083` 至 `20260811_0091`。
- 该数据域继续由 `FEATURE_BID_ASSESSMENT_V1_RUNTIME=false` 默认关闭，不替换旧 `bid_intake_*` 业务入口。
- 只有迁移链、契约、API、幂等、事件、清理任务和相邻回归全部通过后，代码才可随发布候选进入镜像；本次上线不得打开该功能开关。

## 明确排除

- `AI_Middle_Office/.tmp/` 下的本地测试文件、临时 Excel 和覆盖包。
- `video-promo-qisheng/` 视频制作工程、浏览器 Profile、缓存、截图、音频和成片。
- `.env`、密钥、数据库文件、日志、备份、测试缓存、Node/Python 依赖目录和用户上传文件。
- 未通过自动化测试或未完成动态验收的实验性开关。

## 本地候选验收记录

- 隔离数据库：将已校验备份 `pre_0081_pricing_snapshot_recovery_20260801_092701.sql` 恢复到仅监听 `127.0.0.1:33307` 的临时 MySQL；备份大小 `809,317,170` bytes，SHA-256 为 `64DC1AA14C68DBB474B1C02031C051551FFE1EB4991CB88AFD3CCADF891580A3`。
- 迁移预演：恢复库实际 head 为 `20260731_0080`，顺序执行 `0081` 至 `0091` 全部成功；迁移前后项目、定额版本、定额主项和明细数量一致。
- 浏览器动态验收：在项目 `23` 的隔离副本中完成专业全字段布局、定额新增/删除/替换、三级明细展开/收起、明细紧跟所点击定额、企业定额库关闭、当前 active 版本和“待处理”定位验证。
- 金额联动：动态验收发现并修复“混合旧版拆分数据与新编辑数据时只累计部分税额”的问题。新增定额后不含税合计从 `7,012.55` 变为 `7,363.45`，含税合计正确变为 `8,026.16`；删除后分别精确恢复为 `7,012.55` 和 `7,643.68`。
- 企业定额定位：点击“查看企业定额库”后，本地服务收到带当前 active 版本 `8` 和目标条目 `3930` 的新页面请求，目标条目为 `AZ00001`。
- 旧版 Excel：真实 `.xls` 样本可解析出 `20` 条报价项；标准化解析识别 `3` 个 Sheet、`21` 条标准行。
- 原格式导出：隔离环境故意关闭 MinIO，因此完整 HTTP 导出接口按设计返回“MinIO storage is disabled”；绕过存储层、使用同项目历史原格式工作簿执行真实渲染后，装修 `80` 行、机电 `47` 行全部写入，两个工作表均命中且无 `WORKSHEET_NOT_FOUND`。
- 构建与静态门禁：Vite 生产构建通过（`2224` modules），`compileall`、`git diff --check` 通过，Alembic 唯一 head 为 `20260811_0091`。后端全量回归按测试文件互斥拆分为四个隔离分片执行，分别通过 `362`、`353`、`383`、`345` 项，合计 `1443 passed / 0 failed`；`pytest --collect-only` 同步确认仓库共收集 `1443` 项测试，无重复或遗漏。

## 发布门禁

1. `git diff --check`、后端全量测试、前端构建和关键脚本语法检查全部通过。
2. 登录/首次改密、账号授权、项目报价、定额增删替换、价格统计、Excel 导入导出完成本地动态验收。
3. 生成不可变生产镜像、依赖锁、SBOM、漏洞扫描结果和 SHA-256。
4. 只读核对 ECS 当前镜像、Alembic head、唯一 Worker、健康状态和磁盘容量。
5. 正式数据库全量备份、SHA-256 校验和恢复演练完成后，才允许执行 `0083` 至 `0091`；若本次决定不部署隔离数据域，则发布方案必须明确保持正式数据库在 `0082` 并验证关闭开关下的兼容性。
6. API 与 Worker 使用同一镜像版本；自动迁移保持关闭。
7. 上线后完成健康检查和关键业务冒烟；任一门禁失败立即停止并按发布前镜像与数据库备份回滚。

## 尚未授权的动作

- 未创建本地发布提交或版本标签。
- 未推送 GitHub。
- 未连接或修改正式 ECS。
- 未备份、迁移或写入正式数据库。
- 未重启正式 API、Worker、Nginx、MySQL、Redis、MinIO、RAG、n8n 或 Milvus。
