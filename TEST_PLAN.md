# AI 智能报价中台 — 功能测试文档

> 版本：v1.0 | 日期：2026-04-22  
> 测试入口：`http://localhost:9000/`  
> 前提：CentOS 服务已启动（`docker compose up -d`），Windows FastAPI 已运行

---

## 测试账号

| 账号 | 密码 | 角色 | 用途 |
|------|------|------|------|
| admin | （已修改） | 管理员 | 测试管理功能 |
| testuser | 123456 | 业务员 | 测试普通用户视角 |

> testuser 需在注册页提前创建。

---

## 模块一：认证与安全

### T01 — 登录成功
**步骤：** 打开 `http://localhost:9000/`，输入正确账号密码，点击登录  
**预期：** 跳转进入系统主界面，顶部显示用户名和角色，左侧菜单可见

---

### T02 — 登录失败
**步骤：** 输入错误密码，点击登录  
**预期：** 弹出"用户名或密码错误"提示，停留在登录页

---

### T03 — Token 持久化（刷新不掉线）
**步骤：** 登录后按 F5 刷新页面  
**预期：** 直接进入系统，无需重新登录

---

### T04 — 权限隔离（非 admin 看不到管理菜单）
**步骤：** 用 testuser 登录  
**预期：** 左侧菜单只有"业务工作台"，无"知识库管理"选项

---

### T05 — admin 强制修改初始密码
**步骤：**  
1. 用 `create_admin.py` 重置 admin 密码为 `123`（或直接修改数据库）  
2. 重启 FastAPI 服务  
3. 用 admin / 123 登录  

**预期：** 登录成功后立即弹出"请修改初始密码"弹窗，不能关闭，必须填写并提交后才能正常使用

---

### T06 — 修改密码验证
**步骤（接 T05）：**  
1. 弹窗中输入原密码（123）、新密码（少于6位，如"abc"）→ 点击确认  
2. 再次输入原密码（123）、新密码与确认密码不一致 → 点击确认  
3. 正确填写原密码 + 新密码（≥6位）+ 确认密码一致 → 点击确认  

**预期：**  
- 步骤1：提示"新密码不能少于6位"  
- 步骤2：提示"两次输入的密码不一致"  
- 步骤3：提示"密码修改成功"，弹窗关闭，正常进入系统

---

### T07 — 退出登录
**步骤：** 点击右上角用户名 → 退出登录  
**预期：** 返回登录页，localStorage 中 token 被清除；再次刷新仍停留在登录页

---

## 模块二：业务工作台 — AI 测算

### T08 — 文字输入基础流程
**步骤：**  
1. 登录后进入业务工作台  
2. 在输入框中输入：`客厅直线型吊顶50平米，铺地砖80平米，墙面刮腻子120平米`  
3. 点击"发送至 AI 中枢"  

**预期：**  
- 消息气泡出现，逐步显示各阶段进度（唤醒网关 → N8N 处理 → RAG 检索 → AI 优化）  
- 约 15~30 秒后弹出"AI 报价明细预审"弹窗  
- 弹窗中包含 3 个施工项目行，每行有单价和合计

---

### T09 — 图片上传识别流程
**步骤：**  
1. 准备一张包含施工项目清单的图片（截图或拍照）  
2. 点击输入框左侧的 📎 按钮，选择图片  
3. 可在文字框补充说明，点击发送  

**预期：**  
- 消息区域显示图片缩略图  
- GLM-4V 识别阶段有进度提示  
- 最终弹出预审弹窗，施工项目从图片中提取

---

### T10 — 预审面板手工修改
**步骤（接 T08 或 T09）：**  
1. 预审弹窗弹出后，修改某一行的"AI 建议单价"  
2. 修改某一行的"系统合计"  
3. 修改某一行的"施工项目"名称  
4. 观察右下角"人工核定总价"  

**预期：**  
- 所有字段均可编辑  
- 修改合计后，人工核定总价实时更新

---

### T11 — 确认下发
**步骤（接 T10）：**  
1. 点击"✅ 确认无误，下发生成 Excel 并推钉钉"  

**预期：**  
- 显示加载中状态  
- 成功后提示"确认成功！最终报价单已推送到钉钉！"  
- 弹窗关闭，消息区出现"✅ [Human-in-the-Loop] 人工复核确认完毕"  
- 钉钉群收到消息（若已配置）

---

### T12 — 打回重填
**步骤：**  
1. 弹出预审弹窗后，点击"打回重填"  

**预期：**  
- 弹窗关闭  
- 消息区出现"⏸️ 人工审核不通过，流程已打回"  
- 输入框恢复可用，可重新输入

---

### T13 — 失败重试机制
**步骤：**  
1. 断开 CentOS 网络（或停止 N8N），发送一条消息  
2. 等待请求超时或报错  

**预期：**  
- 错误消息气泡中显示错误信息  
- 气泡下方出现"🔄 重试"按钮  
- 点击重试后，自动重新发送相同内容  
- 恢复网络后，重试成功正常流转

---

### T14 — 额度耗尽限制
**步骤：**  
1. 在 admin 面板将 testuser 的额度设为 0  
2. 用 testuser 登录后发送消息  

**预期：** 提示额度不足，无法发送

---

## 模块三：报价历史记录

### T15 — 查看历史记录
**步骤：**  
1. 完成至少一次 T11（确认下发）  
2. 点击输入区左下角"📋 历史记录"按钮  

**预期：**  
- 右侧弹出历史记录抽屉  
- 列表中显示刚才的记录（时间、项目数、总价）

---

### T16 — 查看历史明细
**步骤：** 在历史记录列表中点击某条记录的"详情"  
**预期：** 弹出明细弹窗，显示各施工项目的名称、单价、合计、备注

---

### T17 — admin 查看所有人历史
**步骤：**  
1. 用 testuser 完成一次确认下发  
2. 切换到 admin 账号，打开历史记录  

**预期：** admin 能看到所有用户的记录，包括 testuser 的

---

### T18 — 历史分页
**步骤：** 完成超过 20 次报价后查看历史  
**预期：** 底部显示分页器，可翻页

---

## 模块四：知识库管理（仅 admin）

### T19 — 进入知识库管理
**步骤：** admin 登录后，点击左侧菜单"⚙️ 知识库管理"  
**预期：** 进入管理页面，显示当前 40 条物料表格

---

### T20 — 手动新增物料
**步骤：**  
1. 点击"+ 新增自定义物料"  
2. 填写：项目名称（如"测试项目"）、单价（100）、单位（平米）、备注  
3. 点击"💾 保存表格并核定草稿"  

**预期：** 新行出现在表格中，保存成功提示

---

### T21 — CSV 导入草稿提炼
**步骤：**  
1. 准备一个 CSV 文件，包含列：`施工项目,单价,单位,备注`  
   - 包含库中已有项目（如"地砖"），但价格偏差 >20%  
   - 包含库中没有的新项目  
2. 点击"导入新历史记录(CSV)提取草稿"，上传该 CSV  

**预期：**  
- 新项目和价格异动项追加到表格末尾，标红显示"新提炼草稿"  
- 已有且价格正常的项目不被重复添加

---

### T22 — 核定草稿
**步骤（接 T21）：**  
1. 修改标红草稿行的单价  
2. 点击"💾 保存表格并核定草稿"  

**预期：** 标红消失，数据正常保存

---

### T23 — 热更新至 Milvus
**步骤：** 点击左侧"一键热更新至 Milvus"  
**预期：**  
- 按钮显示加载中  
- 成功后左侧面板显示最后同步时间  
- 通过 RAG API 验证新物料可被检索到：
  ```bash
  curl -s -X POST http://192.168.88.128:8001/api/v1/retrieve \
    -H "Content-Type: application/json" \
    -d '{"query": "测试项目", "top_k": 3}'
  ```

---

## 模块五：用户配额管理（仅 admin）

### T24 — 查看用户列表
**步骤：** admin 进入知识库管理页，查看顶部"员工账号 & AI 额度管理"面板  
**预期：** 显示所有注册用户的账号、角色、当前额度

---

### T25 — 修改额度
**步骤：**  
1. 找到 testuser，在额度列输入框中输入新值（如 20）  
2. 点击该行的"确认"  

**预期：** 提示修改成功，表格中额度更新为 20

---

## 模块六：RAG 检索服务（接口层）

### T26 — 直接检索接口
**步骤（在 CentOS 或本地执行）：**
```bash
curl -s -X POST http://192.168.88.128:8001/api/v1/retrieve \
  -H "Content-Type: application/json" \
  -d '{"query": "卫生间贴砖", "top_k": 3}'
```
**预期：** 返回 JSON，`data` 数组包含 3 条相关物料，有 `item_name`、`unit_price`、`unit` 字段

---

### T27 — 同义词/口语化检索
**步骤：** 依次检索以下关键词，观察结果相关性：

| 查询词 | 预期召回项目 |
|--------|-------------|
| `铺瓷砖` | 地砖/墙砖相关 |
| `刷墙` | 墙面涂料/腻子相关 |
| `装天花板` | 吊顶相关 |
| `换水管电线` | 水电改造相关 |

**预期：** 各查询均能召回语义相关的项目（jieba + BM25 + 向量混合检索生效）

---

### T28 — /admin/reload 热更新接口鉴权
**步骤：**
```bash
# 错误 secret
curl -s -X POST http://192.168.88.128:8001/admin/reload \
  -H "Content-Type: application/json" \
  -d '{"materials": [], "secret": "wrong_secret"}'
```
**预期：** 返回 401 或 403，拒绝访问

---

## 模块七：系统运维

### T29 — CentOS 服务全量健康检查
**步骤（在 CentOS 执行）：**
```bash
docker ps --format "table {{.Names}}\t{{.Status}}"
```
**预期：** 以下容器全部显示 `Up`：
- `rag-api-service`
- `milvus-standalone`
- `milvus-etcd`（healthy）
- `milvus-minio`（healthy）

---

### T30 — 日志聚合脚本
**步骤（在 CentOS 执行）：**
```bash
/opt/rag_service/show_logs.sh rag
```
**预期：** 实时输出 `rag-api-service` 的日志，Ctrl+C 退出

---

### T31 — 磁盘空间检查
**步骤（在 CentOS 执行）：**
```bash
df -h / && docker system df
```
**预期：** `/` 使用率低于 80%，Docker 镜像无大量悬空（dangling）镜像

---

### T32 — Windows 服务自启验证
**步骤：** 重启 Windows，不手动操作，等待约 30 秒后访问 `http://localhost:9000/`  
**预期：** 直接可以打开登录页，FastAPI 已自动启动

---

## 测试结果记录表

| 编号 | 测试项 | 结果 | 备注 |
|------|--------|------|------|
| T01 | 登录成功 | | |
| T02 | 登录失败 | | |
| T03 | Token 持久化 | | |
| T04 | 权限隔离 | | |
| T05 | 强制修改初始密码 | | |
| T06 | 修改密码验证 | | |
| T07 | 退出登录 | | |
| T08 | 文字输入基础流程 | | |
| T09 | 图片上传识别 | | |
| T10 | 预审面板手工修改 | | |
| T11 | 确认下发 | | |
| T12 | 打回重填 | | |
| T13 | 失败重试机制 | | |
| T14 | 额度耗尽限制 | | |
| T15 | 查看历史记录 | | |
| T16 | 查看历史明细 | | |
| T17 | admin 查看全员历史 | | |
| T18 | 历史分页 | | |
| T19 | 进入知识库管理 | | |
| T20 | 手动新增物料 | | |
| T21 | CSV 导入草稿 | | |
| T22 | 核定草稿 | | |
| T23 | 热更新至 Milvus | | |
| T24 | 查看用户列表 | | |
| T25 | 修改用户额度 | | |
| T26 | RAG 直接检索 | | |
| T27 | 同义词检索质量 | | |
| T28 | reload 接口鉴权 | | |
| T29 | 容器健康检查 | | |
| T30 | 日志聚合脚本 | | |
| T31 | 磁盘空间检查 | | |
| T32 | Windows 服务自启 | | |
