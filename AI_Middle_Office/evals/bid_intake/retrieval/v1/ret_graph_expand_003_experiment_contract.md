# RET-GRAPH-EXPAND-003 — 显式清单引用的确定性解析

## 单变量

在002表头规则基础上，只修改显式清单引用的目标召回表达：

- 从来源证据提取清单编号及原文中已有的代码型限定符；
- 使用“编号 + 限定符”做exact召回，不再加入目标行通常不存在的“清单序号”描述词；
- 候选仍必须同时通过目标文档角色、首单元格编号和限定符复核；
- 没有限定符时保守不建立确定性引用边，避免多Sheet重复序号产生错误关系。

表头规则、触发器、图预算、Top5、候选深度、其他检索增强和LLM预算均不变。

## 可证伪假设

显式引用的来源证据已经给出编号和限定符时，使用目标行真实存在的标识符可恢复被普通描述性查询遗漏的目标行。与002的表头收紧组合后，应至少改善Q004和Q005，并继续满足路径精度、无退化、延迟和错误门槛。

## 验收

继续使用原冻结Development B和原门槛，不修改数据集、Gold、分组或阈值。事实门无样本时记为`not_evaluable`。单项目通过仍不授权生产启用，旧Holdout和Challenge不运行。
