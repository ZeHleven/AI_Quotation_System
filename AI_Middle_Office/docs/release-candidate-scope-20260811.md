# 2026-08-11 非研判 Agent 正式发布候选范围

## 当前结论

本候选仅承载已经在 Windows 本地环境完成动态验收的报价、定额、权限、登录、Excel 和生产安全改动。报价资料研判 Agent 的新功能、API、配置、任务、契约及数据库迁移全部排除；即使功能开关默认关闭，也不允许进入本候选镜像或 ECS。

候选代码 Alembic 唯一 head 必须保持为 `20260808_0082`，与正式 ECS 最近一次只读确认的数据库 head 一致。应用自动迁移保持关闭，本次发布不执行数据库升级。

## 纳入候选的用户可见功能

- 项目报价页面布局、清单项/定额项/明细项三级交互及明细字段编辑。
- 项目定额多选新增、删除、替换及金额、总计和统计信息联动。
- 企业定额主库悬浮查看、定额匹配查看、当前 active 版本选择和工料机明细解析。
- 人工、主材、辅材、机械子行识别和 `.xls/.xlsx/.xlsm` 导入解析。
- 原格式报价 Excel 导出、工作表识别和统计导出修复。
- 报价来源展示、企业定额定位和项目报价页面紧凑布局。
- 账号功能授权增删、实际可用功能展示和角色兼容处理。
- 新用户首次登录强制改密，以及业务页面收到 `PASSWORD_CHANGE_REQUIRED` 时自动进入改密窗口并保留原目标地址。
- 报价金额一致性、并发保护、生产配置校验、日志脱敏和单 ECS 部署安全文件。

## 明确排除

- 报价资料研判 Agent 新数据域 `20260810_0083` 至 `20260811_0091`。
- 新研判 Agent 的 `bid_assessment` API、模型、服务、Celery 任务、Outbox/SSE、上传批次、契约、测试和环境变量。
- 另一个开发任务尚未提交或尚未验收的文件。
- 本地临时文件、测试 Excel、覆盖包、视频工程、浏览器 Profile、缓存、截图、音频和成片。
- `.env`、密钥、数据库文件、日志、备份、测试缓存、依赖目录和用户上传文件。

## 已有本地验收依据

- 浏览器动态验收覆盖专业全字段布局、定额新增/删除/替换、三级明细展开/收起、明细紧跟所点击定额、企业定额库关闭、当前 active 版本和“待处理”定位。
- 金额联动验收覆盖新增、删除、替换后定额项、清单项、总计与统计信息同步变化。
- 真实 `.xls` 样本解析出 20 条报价项；标准化解析识别 3 个 Sheet、21 条标准行。
- 原格式工作簿渲染时装修 80 行、机电 47 行均写入正确工作表，无 `WORKSHEET_NOT_FOUND`。

## 本地发布工程复核（2026-08-11）

- 专项后端回归合计 `271 passed`；未执行识图、OCR、图纸分析或报价资料研判 Agent 测试。
- `ai-web` 生产构建通过，Python 编译检查、`git diff --check` 和 Alembic 唯一 head 检查通过。
- 本地候选镜像：`ai-middle-office-app:20260811-nonagent-candidate`。
- 镜像 ID：`sha256:f5d4a3a53d98c6b2b88262dcc5d8d30246d81331553e906fd1de709bad5781aa`；运行用户：`10001:10001`；大小：`113312039` bytes。
- `pypdf` 已从 `6.14.2` 升级至 `6.15.0`；wheel SHA-256 `14e001d6504822cb1ca9c7ed9a69bccb320f59b320730f55af804361abe4d5ee` 已在固定 Python 3.11/Alpine 3.23 镜像中对照 PyPI 官方元数据校验，镜像内基础 PDF 读写冒烟通过。
- 生产依赖锁 SHA-256：`CB9772D965DD8FEB42E330F369123FACA391392DF5DAF9D93C5F6673402DC6DE`。
- SPDX SBOM SHA-256：`539DD9A78740A93EA48CCCB2E3FD65060642C99ED43BB18EBCB8B4F556A065F4`。
- Grype `0.117.0` 使用本地缓存漏洞库扫描 SBOM；报告 SHA-256：`BFB601330E292F69502EF2ABCA2CFE7A150BB4172C2F987630035D01F1BD5AE4`。修复前的两项 `pypdf` Medium 已清零。

当前扫描仍报告基础镜像包的 2 个 High 和 3 个 Medium：

- `CVE-2026-11822`、`CVE-2026-11824`：`sqlite-libs 3.51.2-r0`，均涉及 FTS5 处理恶意 SQLite 数据库；镜像内 `ENABLE_FTS5=True`，不是单纯版本误报。
- `CVE-2025-60876`：`busybox`、`busybox-binsh`、`ssl_client 1.37.0-r30` 三个包匹配，实质为同一基础包漏洞。
- Alpine v3.23 stable 当前仍只提供上述版本，尚无可直接升级的修复包。正式部署配置使用 MySQL，应用代码没有接收并打开用户 SQLite 数据库的业务路径，因此 FTS5 漏洞在当前生产路径不可达；但在 Alpine stable 提供修复前，不能把镜像扫描结果记录为零漏洞。

安全门禁状态：在发布负责人明确接受上述基础镜像已知风险，或改用含修复版本且重新通过专项回归的稳定基础镜像之前，不执行 ECS 部署。

## 本轮门禁

1. 只执行本次改动相关的专项后端测试、前端生产构建、Python 编译和 `git diff --check`；非必要不跑后端全量测试。
2. 不执行识图、OCR、图纸分析或报价资料研判 Agent 的测试、模型调用和数据写入；如确有需要，必须先询问用户。
3. 校验 Alembic 只有一个 head，且为 `20260808_0082`。
4. 构建新的不可变生产镜像，生成 SHA-256、SBOM，并完成不向外部上传代码或镜像元数据的漏洞扫描。
5. 上线前只读核对 ECS 当前镜像、数据库 head、唯一 Worker、健康状态和磁盘容量。
6. API 与 Worker 使用同一镜像；不执行数据库迁移。
7. ECS 上的命令由用户手动执行；上线后仅做登录、权限、项目报价、定额联动和 Excel 导入导出的专项验收。

## 当前授权边界

- 可以在独立本地工作树整理、测试、提交并建立新的非 Agent 发布候选。
- 不连接、不修改、不重启 ECS，不写正式数据库。
- 正式部署前必须再次向用户交付经过核对的手动命令和回滚点。
